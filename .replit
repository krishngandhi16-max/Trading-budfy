run = "npm start"
entrypoint = "src/server.js"
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["node", "src/server.js"]
build = ["sh", "-c", "npx tsc --skipLibCheck"]
deploymentTarget = "cloudrun"
